apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Per-Service MetalLB IP patches (172.16.16.221-226) get added here as each
# app migrates — see readme/gitops-migration.md Phase 5.5.

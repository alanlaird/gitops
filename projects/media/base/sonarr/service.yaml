apiVersion: v1
kind: Service
metadata:
  name: sonarr
  namespace: media
  annotations:
    metallb.io/loadBalancerIPs: "172.16.16.224"
spec:
  type: LoadBalancer
  selector: { app: sonarr }
  ports:
    - port: 8989
      targetPort: 8989

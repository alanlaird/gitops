apiVersion: v1
kind: Service
metadata:
  name: homepage
  namespace: media
  annotations:
    metallb.io/loadBalancerIPs: "172.16.16.227"
spec:
  type: LoadBalancer
  selector: { app: homepage }
  ports:
    - port: 80
      targetPort: http
